import '/imports/ui'; // eslint-disable-line

// if you are using flow router
// this is to start routing only after all components are loaded.
// FlowRouter.wait();


// You can add a loader here to hide the contents until everything is loaded


document.addEventListener('WebComponentsReady', () => {

  // If you are using flow router initialize it here
  // FlowRouter.initialize({
  // });

  // disable the loader.

});
